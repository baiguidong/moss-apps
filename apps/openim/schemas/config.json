{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "apiUrl": {
      "type": "string",
      "title": "OpenIM API URL",
      "description": "OpenIM API 地址，例如 https://im.example.com/api。"
    },
    "wsUrl": {
      "type": "string",
      "title": "OpenIM WebSocket URL",
      "description": "OpenIM WebSocket 地址，例如 wss://im.example.com/msg_gateway。"
    },
    "namespace": {
      "type": "string",
      "title": "用户命名空间",
      "description": "用于在共享 OpenIM 服务中隔离 Moss 实例的稳定标识。",
      "default": "moss"
    },
    "adminUserId": {
      "type": "string",
      "title": "OpenIM 管理员用户 ID",
      "default": "imAdmin"
    },
    "requestTimeoutMs": {
      "type": "integer",
      "title": "请求超时（毫秒）",
      "minimum": 1000,
      "maximum": 60000,
      "default": 15000
    }
  }
}
